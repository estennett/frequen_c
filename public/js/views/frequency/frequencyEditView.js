// Form to create AND edit frequency.
// Shows "new" keywords if creating new frequency
// Shows "edit" keywords if editing existing frequency
// Fills content with existing db info if editing existing frequency
